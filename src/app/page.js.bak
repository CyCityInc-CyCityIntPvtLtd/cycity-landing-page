"use client";
import React from 'react';
import { motion } from 'framer-motion';
import Hero from '@/components/Hero';
import ThreeDCanvas from '@/components/ThreeDCanvas';
import Image from 'next/image';
import { Shield, Zap, Cpu, Lightbulb, Lock, Leaf, Check } from 'lucide-react';

const fadeInUp = {
  initial: { opacity: 0, y: 30 },
  whileInView: { opacity: 1, y: 0 },
  viewport: { once: true },
  transition: { duration: 0.8 }
};

export default function Home() {
  return (
    <div className="flex flex-col">
      <Hero />

      {/* ABOUT US */}
      <section id="section-about" className="py-24 bg-[#050505]">
        <div className="container mx-auto px-4">
          <div className="flex flex-col lg:flex-row items-center gap-16">
            <motion.div
              {...fadeInUp}
              className="lg:w-7/12"
            >
              <span className="text-brand uppercase tracking-widest text-sm mb-4 block">About Us</span>
              <h2 className="text-3xl md:text-5xl font-bold mb-8 leading-tight">
                At Cycity, we are not merely advancing AI; we are meticulously engineering the intelligent
                foundation that will shape the <span className="text-brand">future of India.</span>
              </h2>
              <p className="text-xl text-gray-300 mb-8 font-light">
                Cycity International Pvt. Ltd. is an Indian deep-tech company advancing Artificial
                General Intelligence (AGI) through Quantum Technology.
              </p>
              <ul className="space-y-6">
                {[
                  "We develop intelligent, energy-efficient systems that merge AI, Quantum Computing, and Photonic Innovation.",
                  "With a mission to build technology In India, For India, and For the World.",
                  "At Cycity International, we are not just advancing AI — we are engineering the intelligent foundation of tomorrow’s India."
                ].map((item, idx) => (
                  <li key={idx} className="flex items-start">
                    <div className="bg-brand/20 p-1 rounded-full mr-4 mt-1">
                      <Check className="text-brand" size={16} />
                    </div>
                    <span className="text-gray-400">{item}</span>
                  </li>
                ))}
              </ul>
            </motion.div>
            <motion.div
              initial={{ opacity: 0, scale: 0.8, rotate: -5 }}
              whileInView={{ opacity: 1, scale: 1, rotate: 0 }}
              viewport={{ once: true }}
              transition={{ duration: 1, ease: "easeOut" }}
              className="lg:w-5/12 relative aspect-square w-full"
            >
              <div className="absolute inset-0 bg-brand/10 rounded-full blur-3xl animate-pulse"></div>
              <Image
                src="/images/misc/123.png"
                alt="Innovation"
                fill
                className="object-contain drop-shadow-[0_0_30px_rgba(204,255,51,0.3)] animate-rotate-slow"
              />
            </motion.div>
          </div>
        </div>
      </section>

      {/* MARQUEE */}
      <div className="bg-brand py-6 overflow-hidden border-y border-brand/20">
        <motion.div
          animate={{ x: [0, -1000] }}
          transition={{ repeat: Infinity, duration: 20, ease: "linear" }}
          className="flex whitespace-nowrap items-center space-x-12"
        >
          {[1, 2, 3, 4].map((i) => (
            <React.Fragment key={i}>
              <span className="text-black text-2xl font-bold uppercase tracking-widest">Quantum Rise</span>
              <span className="text-black/30 text-3xl">/</span>
              <span className="text-black text-2xl font-bold uppercase tracking-widest">Intelligent Tomorrow</span>
              <span className="text-black/30 text-3xl">/</span>
              <span className="text-black text-2xl font-bold uppercase tracking-widest">Future Engineered</span>
              <span className="text-black/30 text-3xl">/</span>
              <span className="text-black text-2xl font-bold uppercase tracking-widest">Evolving Systems</span>
              <span className="text-black/30 text-3xl">/</span>
              <span className="text-black text-2xl font-bold uppercase tracking-widest">Infinite Intelligence</span>
              <span className="text-black/30 text-3xl">/</span>
            </React.Fragment>
          ))}
        </motion.div>
      </div>

      {/* TECHNOLOGIES */}
      <section id="section-why-attend" className="py-24 bg-black border-b border-white/5">
        <div className="container mx-auto px-4">
          <div className="text-center max-w-3xl mx-auto mb-16">
            <span className="text-brand uppercase tracking-widest text-sm mb-4 block">Technologies</span>
            <h2 className="text-3xl md:text-5xl font-bold mb-6">Quantum Begins Here</h2>
            <p className="text-gray-400 text-lg">Hear from global AI pioneers & bold thinkers shaping the future.</p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {[
              { title: "AGI", desc: "AGI is the fusion of computation and cognition — able to learn and reason across any domain.", img: "/images/misc/31413.jpg", icon: <Cpu /> },
              { title: "Quantum Computing", desc: "The technology that transforms bits into qubits to unlock infinite parallelism.", img: "/images/misc/342342.JPG", icon: <Zap /> },
              { title: "Quantum AI", desc: "Hybrid AI models powered by quantum computing for self-evolving intelligence.", img: "/images/misc/s5.JPG", icon: <Lightbulb /> },
              { title: "Electro-Photonic", desc: "Next-gen hardware merging photonics & electronics for ultra-fast computing.", img: "/images/misc/6.jpg", icon: <Cpu /> },
              { title: "Secure AI", desc: "Quantum-safe encrypted frameworks ensuring privacy & trust.", img: "/images/misc/7.jpeg", icon: <Lock /> },
              { title: "Sustainable", desc: "Carbon-neutral, energy-optimized AI systems.", img: "/images/misc/8.jpg", icon: <Leaf /> },
            ].map((tech, idx) => (
              <motion.div
                key={idx}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                viewport={{ once: true }}
                transition={{ delay: idx * 0.1 }}
                className="group relative overflow-hidden rounded-xl aspect-[4/5] bg-gray-900"
              >
                <Image
                  src={tech.img}
                  alt={tech.title}
                  fill
                  className="object-cover transition-transform duration-700 group-hover:scale-110 opacity-60"
                />
                <div className="absolute inset-0 bg-gradient-to-t from-black via-black/20 to-transparent group-hover:via-brand/10 transition-colors duration-500"></div>
                <div className="absolute bottom-0 left-0 p-8 w-full transition-transform duration-500 transform translate-y-4 group-hover:translate-y-0">
                  <div className="text-brand mb-4 scale-125 origin-left">{tech.icon}</div>
                  <h4 className="text-2xl font-bold text-white mb-2">{tech.title}</h4>
                  <p className="text-gray-300 text-sm opacity-0 group-hover:opacity-100 transition-opacity duration-500">{tech.desc}</p>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* QUOTE SECTION */}
      <section className="py-24 relative overflow-hidden">
        <div className="absolute inset-0 z-0">
          <Image src="/images/background/aa.png" alt="BG" fill className="object-cover opacity-20" />
        </div>
        <div className="container mx-auto px-4 relative z-10">
          <div className="flex flex-col md:flex-row items-center gap-12 bg-black/40 backdrop-blur-md p-12 rounded-2xl border border-brand/10">
            <div className="md:w-1/3 relative aspect-square w-64">
              <Image src="/images/misc/mer.jpg" alt="Merwyn" fill className="object-cover rounded-xl border-2 border-brand/20" />
              <div className="absolute -top-4 -right-4 bg-brand text-black p-4 rounded-xl font-serif text-4xl">“</div>
            </div>
            <div className="md:w-2/3">
              <h3 className="text-2xl md:text-3xl italic font-light text-white mb-6 leading-relaxed">
                “Artificial intelligence is advancing rapidly, and while it offers immense opportunity, it
                also poses significant risks.”
              </h3>
              <div className="flex items-center">
                <div className="w-12 h-[1px] bg-brand mr-4"></div>
                <span className="text-brand font-bold uppercase tracking-widest">Merwyn Furtado</span>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* TEAM */}
      <section id="section-speakers" className="py-24 bg-[#050505]">
        <div className="container mx-auto px-4">
          <div className="text-center max-w-3xl mx-auto mb-16">
            <span className="text-brand uppercase tracking-widest text-sm mb-4 block">Team</span>
            <h2 className="text-3xl md:text-5xl font-bold mb-6">Meet the Visionaries</h2>
            <p className="text-gray-400 text-lg">In the realm of innovation, visionaries are the architects of tomorrow.</p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {[
              { name: "Merwyn Furtado", role: "CEO and Founder", img: "/images/team/mer.jpg" },
              { name: "Raktim Banerjee", role: "System Embedded Engineer", img: "/images/team/r.jpg" },
              { name: "Dishik Setty", role: "Software Engineer", img: "/images/team/d.jpg" },
            ].map((member, idx) => (
              <motion.div
                key={idx}
                {...fadeInUp}
                transition={{ delay: idx * 0.2 }}
                className="group relative overflow-hidden rounded-2xl aspect-[3/4] bg-gray-900 shadow-2xl"
              >
                <Image
                  src={member.img}
                  alt={member.name}
                  fill
                  className="object-cover grayscale group-hover:grayscale-0 transition-all duration-700"
                />
                <div className="absolute inset-0 bg-gradient-to-t from-black via-transparent to-transparent opacity-80 group-hover:opacity-60 transition-opacity"></div>
                <div className="absolute bottom-0 left-0 w-full p-8 text-center bg-black/60 backdrop-blur-sm transform transition-transform duration-500 translate-y-2 group-hover:translate-y-0">
                  <h3 className="text-2xl font-bold text-brand uppercase tracking-wider">{member.name}</h3>
                  <p className="text-gray-300 text-sm">{member.role}</p>
                </div>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* OUR VISIONS */}
      <section id="section-venue" className="py-24 bg-black border-y border-brand/5">
        <div className="container mx-auto px-4">
          <div className="text-center mb-16">
            <span className="text-brand uppercase tracking-widest text-sm mb-4 block">Our Visions</span>
            <h2 className="text-3xl md:text-5xl font-bold">Quantum Intelligent India</h2>
          </div>

          <div className="space-y-24">
            {[
              {
                title: "A vision to build an India empowered by quantum-driven intelligence…",
                img: "/images/misc/ele.png",
                reverse: false
              },
              {
                title: "A commitment to engineering technologies that power intelligent systems…",
                img: "/images/misc/last.jpg",
                reverse: true
              }
            ].map((vision, idx) => (
              <div key={idx} className={cn("flex flex-col md:flex-row items-center gap-16", vision.reverse && "md:flex-row-reverse")}>
                <motion.div
                  {...fadeInUp}
                  className="md:w-1/2"
                >
                  <h2 className="text-3xl md:text-5xl font-light leading-snug">
                    {vision.title}
                  </h2>
                </motion.div>
                <motion.div
                  initial={{ opacity: 0, scale: 0.9 }}
                  whileInView={{ opacity: 1, scale: 1 }}
                  viewport={{ once: true }}
                  className="md:w-1/2 relative aspect-video w-full rounded-2xl overflow-hidden"
                >
                  <Image src={vision.img} alt="Vision" fill className="object-cover" />
                  <div className="absolute inset-0 bg-brand/10 mix-blend-overlay"></div>
                </motion.div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* FAQ */}
      <section className="py-24 bg-[#050505]">
        <div className="container mx-auto px-4">
          <div className="flex flex-col lg:flex-row gap-16">
            <div className="lg:w-1/3">
              <span className="text-brand uppercase tracking-widest text-sm mb-4 block">FAQ</span>
              <h2 className="text-3xl md:text-5xl font-bold mb-6">Frequently Asked Questions</h2>
              <p className="text-gray-400">Everything You Need to Know about the future of intelligence.</p>
            </div>
            <div className="lg:w-2/3 space-y-4">
              {[
                { q: "How is AGI different from traditional AI?", a: "Traditional AI is task-specific, while AGI can generalize knowledge across any domain, mimicking human cognition." },
                { q: "Why is Quantum Computing revolutionary?", a: "Quantum computers process many possibilities at once through qubits, unlocking infinite parallelism for complex problem-solving." },
                { q: "What is Photonic Computing?", a: "Photonic computing uses light (photons) instead of electricity (electrons) to move data at much higher speeds with lower energy consumption." },
                { q: "How does Photonic Computing benefit AI?", a: "It enables ultra-fast data movement and massive parallelism, which are critical for training next-gen AI models." },
                { q: "What is Quantum-Safe Encryption?", a: "It refers to encryption algorithms that are secure against attacks from both classical and future quantum computers." }
              ].map((faq, idx) => (
                <details key={idx} className="group border-b border-white/10 pb-4 cursor-pointer">
                  <summary className="flex justify-between items-center text-xl font-medium text-white hover:text-brand transition-colors list-none">
                    {faq.q}
                    <span className="text-brand transition-transform group-open:rotate-180">+</span>
                  </summary>
                  <p className="mt-4 text-gray-400 leading-relaxed max-w-2xl">{faq.a}</p>
                </details>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* 3D HIGHLIGHT SECTION */}
      <section className="h-[60vh] relative bg-black flex items-center justify-center overflow-hidden">
        <ThreeDCanvas className="absolute inset-0 z-0 opacity-40" />
        <div className="relative z-10 text-center container mx-auto px-4">
          <motion.h2
            {...fadeInUp}
            className="text-4xl md:text-7xl font-bold mb-8 uppercase tracking-[0.2em]"
          >
            The Future is <span className="text-brand">Quantum</span>
          </motion.h2>
          <motion.p
            {...fadeInUp}
            transition={{ delay: 0.2 }}
            className="text-xl text-gray-400 max-w-2xl mx-auto"
          >
            Building the bridge between human ingenuity and artificial general intelligence.
          </motion.p>
        </div>
      </section>

      {/* NEWSLETTER */}
      <section className="py-24 bg-brand relative overflow-hidden">
        <div className="container mx-auto px-4 relative z-10 text-center">
          <div className="max-w-3xl mx-auto">
            <span className="text-black/60 uppercase tracking-widest text-sm mb-4 block font-bold">Stay in the Loop</span>
            <h2 className="text-3xl md:text-5xl font-black text-black mb-12 uppercase italic">Join the Future of Innovation</h2>
            <form className="flex flex-col md:flex-row gap-4">
              <input
                type="email"
                placeholder="Enter Your Email Address"
                className="flex-1 bg-black/10 border-2 border-black/20 px-6 py-4 rounded-sm text-black placeholder:text-black/50 focus:outline-none focus:border-black transition-colors font-medium"
              />
              <button className="bg-black text-brand px-10 py-4 font-black uppercase tracking-widest hover:bg-white hover:text-black transition-all">
                SIGN UP
              </button>
            </form>
            <p className="mt-6 text-black/40 text-xs">
              Note: You can opt out at any time. See our Privacy Policy, Terms, and Cookies.
            </p>
          </div>
        </div>
        {/* Decorative Elements */}
        <div className="absolute top-0 right-0 w-64 h-64 bg-white/10 blur-3xl -mr-32 -mt-32"></div>
        <div className="absolute bottom-0 left-0 w-64 h-64 bg-black/5 blur-3xl -ml-32 -mb-32"></div>
      </section>
    </div>
  );
}

// Helper for conditional classes
function cn(...classes) {
  return classes.filter(Boolean).join(' ');
}
